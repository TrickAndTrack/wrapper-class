Convert Java Boolean object---->Boolean blnObj = new Boolean("true");
 boolean primitive example---->boolean b = blnObj. booleanValue();  
                               byte b = byteVal.byteValue();
							   
Convert Java Boolean object---->Boolean blnObj = new Boolean("true");
                           --->boolean b1=true;
String object ---------------->String s1=String.valueOf(b1);

-------------------------------------------------------------------------------------------------------------------------------

String Object----------------->String s1="true";  

to 
Boolean Object---------------->boolean b1=Boolean.parseBoolean(s1);


-------------------------------------------------------------
boolean Primitive------------->    boolean bvar = true;
 to
 
----------------------------------------------------------------------------------------------------------------------------------
Convert Byte object-------------->Byte bObj = new Byte("10");
 to 
String object------------------->String str = bObj. toString();


Convert Byte --------------------->Byte byteVal = new Byte("35");
 to 
 
numeric primitive data types------>byte b = byteVal.byteValue(); 



Convert String ------------>byte b = byteVal.byteValue();

to

primitive byte Example-----> byte b = Byte.parseByte(str);


Convert byte ----------------->byte i = 10;
to
Byte object Example----------->Byte bObj = new Byte(i);

--------------------------------------------------------------------------------------------------------------------------------

convestion means ----> object into object
-----------------------> veriable into onather veriable
----------------------->object inot simple veriable and vaise versa



convestion object ot veriable---> Integer wholenumber = new Integer(400);
---------------------------------> int dest = wholenumber.intValue();


convestion veriable to object-----> String val = "47";
-----------------------------------> int intObj = Integer.parseInt(val);
-------------------------------------------------------------------------------------------------------------------------------
